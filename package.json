{
  "name": "nestjs-agentic-monorepo",
  "version": "0.2.0",
  "private": true,
  "description": "NestJS-native primitives for building production AI agents.",
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
