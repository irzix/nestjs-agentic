# Changesets

Hello and welcome! This directory contains configuration and pending changesets for **nestjs-agentic**.

When creating a PR or making changes to packages, run `npx changeset` to record your changes.
